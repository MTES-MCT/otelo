import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Retours utilisateurs - Administration Otelo',
}

export default function FeedbacksLayout({ children }: { children: React.ReactNode }) {
  return children
}
